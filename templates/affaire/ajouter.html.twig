{% extends 'base.html.twig' %}

    {% block title %}{{ parent() }}-Ajout Affaire{% endblock %}

    {% block body %}
        <div class="row d-flex justify-content-center">
            <div class="col-md-6">
                <h1>Ajouter une affaire</h1>
                {{ form(monFormulaire) }}
            </div>
        </div>
    {% endblock %}
