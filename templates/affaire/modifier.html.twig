{% extends 'base.html.twig' %}

    {% block title %}{{ parent() }}-Modification Affaire{% endblock %}

    {% block body %}
        <div class="row d-flex justify-content-center">
            <div class="col-md-6">
                <h1>Modifier Affaire</h1>
                {{ form(monFormulaire) }}
            </div>
        </div>
        </div>

    {% endblock %}
