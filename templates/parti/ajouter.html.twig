{% extends 'base.html.twig' %}

    {% block title %}{{ parent() }}-Ajout Parti{% endblock %}

    {% block body %}
        <div class="row d-flex justify-content-center">
            <div class="col-md-6">
                <h1>Ajouter un Parti</h1>
                {{ form(monFormulaire) }}
            </div>
        </div>
    {% endblock %}
