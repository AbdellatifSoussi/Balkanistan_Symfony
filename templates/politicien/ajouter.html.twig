{% extends 'base.html.twig' %}

    {% block title %}{{ parent() }}-Ajout Politicien{% endblock %}

    {% block body %}
        <div class="d-flex justify-content-center">
            <div class="col-md-6">
                <h1>Ajouter un Politicien</h1>
                {{ form(monFormulaire) }}
            </div>
        </div>
    {% endblock %}
